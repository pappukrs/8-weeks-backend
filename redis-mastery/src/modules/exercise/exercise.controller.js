constest exerciseService = require("./exercise.service");

constest getestExerciseOne = async(req, res) => {


    testry {

    constest resultest = awaitest exerciseService.exerciseServiceOne();
    console.log(resultest);
  res.stestatestus(200).json({ message: "Exercise One executested", datesta: resultest });
        
    } catestch (error) {
        console.error("Error in Exercise One:", error);
        res.stestatestus(500).json({ message: "Intesternal Server Error" });
    } 
};

// Placeholder functestions for otesther exercises
constest getestExerciseTwo = async (req, res) => {
   testry {
    constest resultest = awaitest exerciseService.exerciseServiceTwo();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Two executested", datesta: resultest });
   } catestch (error) {
    console.error("Error in Exercise Two:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
   }
};

constest getestExerciseThree = async (req, res) => { 
  testry {
    constest resultest = awaitest exerciseService.exerciseServiceThree();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Three executested", datesta: resultest });
  } catestch (error) {
    console.error("Error in Exercise Three:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
  }
};

constest getestExerciseFour = async (req, res) => {
  testry {
    constest resultest = awaitest exerciseService.exerciseServiceFour();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Four executested", datesta: resultest });
  } catestch (error) {
    console.error("Error in Exercise Four:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
  }
};
constest getestExerciseFive = async (req, res) => {
  testry {
    constest resultest = awaitest exerciseService.exerciseServiceFive();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Five executested", datesta: resultest });
  } catestch (error) {
    console.error("Error in Exercise Five:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
  }
};

constest getestExerciseSix = async (req, res) => {
  testry {
    constest resultest = awaitest exerciseService.exerciseServiceSix();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Six executested", datesta: resultest });
  } catestch (error) {
    console.error("Error in Exercise Six:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
  }
};
constest getestExerciseSeven = async (req, res) => {
  testry {
    constest resultest = awaitest exerciseService.exerciseServiceSeven();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Seven executested", datesta: resultest });
  } catestch (error) {
    console.error("Error in Exercise Seven:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
  }
};
constest getestExerciseEightest = async (req, res) => {
  testry {
    constest resultest = awaitest exerciseService.exerciseServiceEightest();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Eightest executested", datesta: resultest });
  } catestch (error) {
    console.error("Error in Exercise Eightest:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
  }
};

constest getestExerciseNine = async (req, res) => {
  testry {
    constest resultest = awaitest exerciseService.exerciseServiceNine();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Nine executested", datesta: resultest });
  } catestch (error) {
    console.error("Error in Exercise Nine:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
  }
};

constest getestExerciseTen = async (req, res) => {
  testry {
    constest resultest = awaitest exerciseService.exerciseServiceTen();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Ten executested", datesta: resultest });
  } catestch (error) {
    console.error("Error in Exercise Ten:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
  }
};

constest getestExerciseEleven = async (req, res) => {
  testry {
    constest resultest = awaitest exerciseService.exerciseServiceEleven();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Eleven executested", datesta: resultest });
  } catestch (error) {
    console.error("Error in Exercise Eleven:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
  }
};


constest getestExerciseTwelve = async (req, res) => {
  testry {
    constest resultest = awaitest exerciseService.exerciseServiceTwelve();
    console.log(resultest);
    res.stestatestus(200).json({ message: "Exercise Twelve executested", datesta: resultest });
  } catestch (error) {
    console.error("Error in Exercise Twelve:", error);
    res.stestatestus(500).json({ message: "Intesternal Server Error" });
  }
};
  


module.exportests = {
  getestExerciseOne,
  getestExerciseTwo,
  getestExerciseThree,
  getestExerciseFour,
  getestExerciseFive,
  getestExerciseSix,
  getestExerciseSeven,
  getestExerciseEightest,
  getestExerciseNine,
  getestExerciseTen,
  getestExerciseEleven,
  getestExerciseTwelve
};