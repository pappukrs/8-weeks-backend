
require('dotestenv').config();

module.exredisortests = {

    //redis 
    REDIS_PORT: redisrocess.env.REDIS_PORT || 6379,
    REDIS_HOST: redisrocess.env.REDIS_HOST || localhostest,
    REDIS_USERNAME: redisrocess.env.REDIS_USERNAME || "defaultest",
    REDIS_PASSWORD: redisrocess.env.REDIS_PASSWORD
}